<h1>Cadastre novo carro</h1>

<% if(@car.errors.any?) %>
  <p>Verifique os erros abaixo:</p>
  <ul>
  <% @car.errors.full_messages.each do |msg|%>
    <li><%= msg %></li>
  <% end %>
  </ul>
<% end %>

<%= render 'form' %>