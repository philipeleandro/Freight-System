<h1>Sistema de Transportadoras</h1>

<% if user_signed_in? %>
  <% if current_user.kind == 'Regular' %>
    <% if Company.find_by(domain: current_user.email.split("@").last) == nil %>
     <h3>Bem-vindo</h3>
    <% else %>
      <h3>Bem-vindo <%= Company.find_by(domain: current_user.email.split("@").last).brand_name %></h3>
    <% end %>
  <% end %>
<% else %>
  <h3> Bem-vindo visitante </h3>
     <%= form_with(url: search_orders_path, method: :get) do |f| %>
        <%= f.label :query, 'Buscar Pedido' %>
        <%= f.text_field :query %>
        <%= f.submit 'Buscar' %>
    <% end %>
<% end %>