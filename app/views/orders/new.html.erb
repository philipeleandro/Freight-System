<h1>Configure ordem de serviço</h1>

<% if(@order.errors.any?) %>
  <p>Verifique os erros abaixo:</p>
  <ul>
  <% @order.errors.full_messages.each do |msg|%>
    <li><%= msg %></li>
  <% end %>
  </ul>
<% end %>

<%= render 'form' %>

<%= link_to 'Voltar', root_path %>